# SM8750 更新记录：2026-09-09

## 范围

- Linux LTS：6.6.138 → 6.6.156，版本核对时间为 2026-09-09。
- 使用 kernel.org 对应的新旧完整源码发布包进行三方合入，再适配 Samsung/Android 分支差异；这不是把厂商内核替换为 vanilla Linux，也不是导入 stable 仓库全部 Git 历史。
- 移除构建时的 Wild Kernels 性能补丁注入，保留 Unicode 修复、Droidspaces 和原有非性能特性。
- 本次没有推送远端、发布 ZIP 或进行真机刷写。

## 特性来源

| 特性 | 本次状态 |
|---|---|
| ReSukiSU | 固定为 `f1dd81dc96d7f3f6691e6ac8b50fba9ae8a2f17c` |
| SUSFS | 2.3.0，`3f0b811b2e105afd8dc858cd71389090e01f404b` |
| ReKernel | 11.6，`5adec4896a549af60fab2ab59441a777551763b9`，由 LKM-Source 适配为内置编译 |
| ZeroMount 内核补丁 | 继续使用 Super-Builders `c2cb71614868fe742cbffee2b6f3126523432673`；核对时没有更新的内核补丁 |
| ZRAM / LZ4 | 保留原有 LZ4 默认压缩配置；采用本次 LTS 中适用的内核实现更新，没有另行替换压缩库 |
| ipset | 保持启用及原配置；采用本次 LTS 中适用的内核实现更新 |
| Baseband-guard | 保持原有集成方式；本次构建拉取到 `a54e0dc6cf0aff4dd87fec49644a02d2eb612905` |

ReKernel 默认保留 legacy/raw netlink 协议，避免本次升级要求同时替换现有用户态客户端。网络过滤开关仍保持原有默认关闭状态。

来源：[Linux stable](https://www.kernel.org/)、[ReSukiSU](https://github.com/ReSukiSU/ReSukiSU)、[SUSFS](https://github.com/ShirkNeko/susfs4ksu)、[Super-Builders](https://github.com/Enginex0/Super-Builders)、[ReKernel](https://github.com/Sakion-Team/Re-Kernel)。

## 移除的 Wild 性能补丁

- `silence_irq_cpu_logspam.patch`
- `f2fs_enlarge_min_fsync_blocks.patch`
- `f2fs_reduce_congestion.patch`
- `reduce_gc_thread_sleep_time.patch`
- `increase_ext4_default_commit_age.patch`
- `clear_page_16bytes_align.patch`
- `file_struct_8bytes_align.patch`
- `disable_cache_hot_buddy.patch`
- `reduce_cache_pressure.patch`
- `mem_opt_prefetch.patch`
- `optimized_mem_operations.patch`
- `int_sqrt.patch`
- `minimise_wakeup_time.patch`
- `reduce_freeze_timeout.patch`
- `reduce_pci_pme_wakeups.patch`
- `increase_sk_mem_packets.patch`
- `force_tcp_nodelay.patch`
- `add_timeout_wakelocks_globally.patch`
- `avoid_extra_s2idle_wake_attempts.patch`

本次同时移除了全局 wakelock 超时和 s2idle 唤醒抑制补丁。

## 必要的兼容适配

- 保留厂商/Android 钩子及分支专用接口，针对 LTS 冲突做适配，未把厂商差异全部丢弃。
- HID 保留旧的五参数导出接口，新版有界处理走独立接口，避免直接更改现有调用 ABI。
- F2FS 保留厂商锁封装和统计；设备结构的新增字段优先使用现有 KABI 保留区。
- fscrypt 保持原有 get_devices 回调接口，移植上游设备数组分配不得失败的修复语义。
- 保留 arm64 CONTPTE/pKVM 分支实现；exFAT 的 benign secondary entry 保留修复适配到厂商 entry-set cache API，并检查磁盘 SecondaryCount 上限。
- SUSFS 2.3 补丁仍引用此 ReSukiSU 版本没有的 `ksu_install_su_fd()`。兼容补丁仅移除这一过时的 post-exec 步骤，保留 su/exec 钩子；没有拿不同用途的 `ksu_install_fd()` 强行代替。

## 验证结果与限制

| 模式 | arm64 Image 编译 | 编译器 | Image 大小 |
|---|---|---|---|
| LKM | 通过，退出码 0 | clang-r510928 | 38406656 bytes |
| ReSukiSU | 通过，退出码 0 | clang-r510928 | 38672896 bytes |

- 本仓特性集成静态检查通过；新兼容补丁在未适配文件上正向 dry-run、已编译文件上反向 dry-run 均通过（fuzz=0）。
- 两种模式均确认：ZRAM 默认 `lz4`、ipset、ReKernel legacy netlink、NTSync、BBR 和 NTFS3 保留；LKM 不内置 KSU，ReSukiSU 模式启用 KSU/SUSFS/ZeroMount。
- 构建日志位于工作目录 `/home/yucca/work/kernel-update-20260909/sm8750-lkm-final.log` 和 `sm8750-resukisu-final.log`。
- 构建产物来自提交前验证树，版本后缀可能仍包含旧基线短 SHA；它们是编译验证产物，不是正式发布包。
- 没有完成设备启动、厂商模块装载、完整 KABI 检查、传感器或文件系统运行测试。

正式发布前需在目标设备检查：启动与模块装载、首次充电休眠及多次唤醒、双击唤醒、陀螺仪（尤其 SM8550 的 ReSukiSU 模式）、root/挂载隐藏、ReKernel 用户态通信，以及 exFAT 文件创建/重命名/移动。此次更新不能视为已证实修复此前的真机故障。
